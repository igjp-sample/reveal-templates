{
  "name": "reveal-app1",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev": "concurrently \"npm:dev:client\" \"npm:dev:server\"",
    "build:client": "npm run build --workspace=client",
    "build:server": "npm run build --workspace=server",
    "build": "npm run build:client && npm run build:server && npm run build:deploy",
    "build:deploy": "node -e \"require('fs').copyFileSync('server/package.deploy.json', 'dist/package.json')\" && cd dist && npm install --omit=dev && npm rebuild reveal-sdk-node --ignore-scripts=false --foreground-scripts --omit=dev",
    "preview": "npm run start",
    "start": "cd ./dist && node server.js",
    "rebuild:native": "npm rebuild reveal-sdk-node --ignore-scripts=false --foreground-scripts"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
